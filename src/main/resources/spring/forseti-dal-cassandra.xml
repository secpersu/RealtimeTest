<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
						http://www.springframework.org/schema/beans/spring-beans-3.2.xsd 
						http://www.springframework.org/schema/context
       					http://www.springframework.org/schema/context/spring-context-3.2.xsd
						http://www.springframework.org/schema/tx
           				http://www.springframework.org/schema/tx/spring-tx-3.2.xsd
			            http://www.springframework.org/schema/aop 
			            http://www.springframework.org/schema/aop/spring-aop-3.2.xsd"
	default-autowire="byName">

	<context:property-placeholder
		location="classpath:properties/forseti.dal.cassandra.properties"
		ignore-unresolvable="true" />

	<context:annotation-config />

	<!-- ===================== 定义扫描根路径为，不使用默认的扫描方式 =================== -->
	<context:component-scan base-package="cn.fraudmetrix.forseti.dal.cassandra"
		use-default-filters="false">
		<!-- 扫描符合@Service @Repository的类 -->
		<context:include-filter type="annotation"
			expression="org.springframework.stereotype.Service" />
		<context:include-filter type="annotation"
			expression="org.springframework.stereotype.Repository" />
	</context:component-scan>

	<!-- ================================== cassandra 配置 ====================================== -->
	<bean id="cassandraClient"
		class="cn.fraudmetrix.forseti.dal.cassandra.dao.CassandraClient"
		destroy-method="close" init-method="init">
		<property name="Keyspace" value="${forseti.cassandra.keyspace}" />
		<property name="agentHostList" value="${forseti.cassandra.host.list}" />
		<property name="maxconnectionsPerHost" value="${forseti.cassandra.maxconnections.perhost}" />
		<property name="coreconnectionsPerHost" value="${forseti.cassandra.coreconnections.perhost}" />
		<property name="fetchSize" value="${forseti.cassandra.fetchsize}" />
		<property name="usedHostsPerRemoteDc" value="${forseti.cassandra.usedhosts.perremote.dc}" />
		<property name="localDc" value="${forseti.cassandra.local.dc}" />
		<property name="consistency" value="${forseti.cassandra.consistency}"/>
	</bean>

	<bean id="cassandraVelocityWriteDaoImpl"
		class="cn.fraudmetrix.forseti.dal.cassandra.dao.impl.CassandraVelocityWriteDaoImpl"
		init-method="init">
		<property name="expiredSeconds" value="${forseti.cassandra.expired.seconds}" />
	</bean>

</beans>
